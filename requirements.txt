aiohttp>=3.14.3,<4
aiohttp_socks>=0.12.0,<1
# Offline GeoIP country lookups (pure-Python reader; mmdb data is fetched at runtime
# from the community mirror, no MaxMind license key required).
geoip2>=5.3.0,<6
